
<%= form_for @trip_location, url:city_location_path(@location), method: :patch do |f| %>
<%= f.label :trip_id %>
<%= f.collection_select :trip_id, @trips, :id, :name %>
<%= f.label :rating %>
<%= f.number_field :rating %>
<%= f.submit %>
<% end %>