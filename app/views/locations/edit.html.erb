
<%= form_for @trip_location, url:location_path(@location), method: :patch do |f| %>
<%= f.label :trip_id %>
<%= f.collection_select :trip_id, Trip.all, :id, :name %>
<%= f.label :rating %>
<%= f.number_field :rating %>
<%= f.submit %>
<% end %>