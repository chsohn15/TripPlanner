<h1><%= @trip.name %></h1>

<div> Start Date: <%= @trip.start_date.strftime("%B %d, %Y") %></div>
<div> End Date: <%= @trip.end_date.strftime("%B %d, %Y") %></div>
<div> Average Rating: <%= @trip.average_rating.round(2) %></div>
<div> Visited: </div>
<ul>
    <% @trip.locations.each do |location| %>
        <li><%= link_to "#{location.name} - #{location.city.name}", city_location_path(city_id: location.city, id:location)%></li>
    <% end %>
</ul>

<div><%= link_to "Add Locations", countries_path %></div>
<div><%= link_to "Edit Trip", edit_trip_path(@trip) %></div>