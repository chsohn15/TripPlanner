

<%= form_tag(country_states_path(params[:country_id]), method: :get) do %>
    <%= label_tag :country_id, "Select Country"%>
    <%= select_tag(:country_id, options_from_collection_for_select(@countries, :id, :name)) %>
    <%= submit_tag "Search" %>
<% end %>





